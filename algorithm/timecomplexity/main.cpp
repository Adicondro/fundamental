#include <iostream>

using namespace std;

int main (int argc, char *argv[]) {
  
  // O(log n)
  // O(n)
  // O(n^2)


  // O(1)
  int arr[5] = {1,2,3,4,5};

  cout << arr[0] << endl;
  
  return 0;
}
