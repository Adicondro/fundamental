/* 


	1.	Inventory System
	•	Manage items in an inventory (add, remove, calculate total value).
	•	Use arrays, objects, loops, and functions.
    

    1. Task: Inventory Management System

    Description: Write a function that manages a small inventory system for a store.
        •	The inventory is represented as an array of objects, where each object contains the item name, price, and quantity.
        •	Create a function addItem to add a new item to the inventory.
        •	Create a function removeItem to remove an item based on its name.
        •	Create a function calculateTotalValue to calculate the total value of the inventory.

    Requirements:
        •	Use different variable data types (const, let, var).
        •	Use control flow (e.g., if-else or loops).
        •	Use functions effectively.

    Example:

    const inventory = [
        { item: "Apple", price: 2, quantity: 10 },
        { item: "Banana", price: 1, quantity: 20 }
    ];

    addItem("Orange", 1.5, 15);
    removeItem("Banana");
    console.log(calculateTotalValue()); // Should return the total value of the remaining items


*/