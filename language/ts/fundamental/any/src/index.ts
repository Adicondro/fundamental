let nomor = 12;

let anyvariable;

function print(variable : any) { // This is a bad practice
    console.log(variable);
}

// Typescript always ask us to not use any type, because it's not a good practice