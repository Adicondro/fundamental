// console.log; // Global

// setTimeout(); // Global
// clearTimeout(); // Global

// setInterval(); // Global
// clearInterval(); // Global

// semua inni adalah global object, dan bisa diakses dari mana saja, tidak harus global.example

let message = '';
console.log(global.message); // undefined karena message yang di define bukan di global object